#author:MVaouli
import re
import collections
import javax.xml.bind.DatatypeConverter

class HttpFeaturesSnort(inList)

    def HttpIdsRulesCache(inList):

      dl1 = uri.find("/SiteServer/Admin/knowledge/persmbr")
      dl2 =  uri.find("/ncommerce3/ExecMacro/orderdspc.d2w")
      dl3 =  uri.find("/servlet/con")

    def uri_rule_check(uri, pattern):

        rule_match = uri.find(pattern)

        if (rule_match == True):
            return True
        else
            return False

    def HttpIdsUtils(http_uri,http_method,http_header):